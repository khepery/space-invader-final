/*
 * ┌──────────────────────────────────────────────────────────────────┐
 * │  ACCESS CODE CONFIGURATION                                       │
 * │                                                                  │
 * │  To change the access code, edit the "code" value below,        │
 * │  save this file, and share the new code with your students.      │
 * │  That's it — no other file needs to be changed.                 │
 * │                                                                  │
 * │  ⚠ SECURITY NOTE: This code is stored in plain text and is      │
 * │  visible in the browser source. This is intentional — it's a    │
 * │  simple classroom gate, not a security barrier. It prevents      │
 * │  casual/accidental access but is NOT suitable for protecting     │
 * │  sensitive content. For classroom use this is perfectly fine.    │
 * └──────────────────────────────────────────────────────────────────┘
 */
window.SITE_CONFIG = {
  code: "GALAXY2026"
};
